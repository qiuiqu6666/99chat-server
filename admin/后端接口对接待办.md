# IM 管理后台 · 后端接口对接待办清单

面向 **PHP admin-api**。与《**前端对接文档.md**》（主文档）对照：主文档若已写明则以下条可作为 **实现自检**；未实现的仍按本节推进。

---

## 1. `GET /api/v1/users` — 校对

| 事项 | 说明 |
|------|------|
| **`is_online` Query** | 主文档已列：`1`/`0`/不传；**SQL 筛选 + `total` 与分页一致**。 |
| **`friend_count` / `group_count`** | 与子查询 LEFT JOIN 一致。 |

---

## 2. `GET /api/v1/users/detail` — 用户详情页 P0

见 **`好友列表页-Java后端待办.md` §11**（含 P1/P2 子接口与联调 checkbox）。当前 Java 端 **404**，阻塞 `/im-admin/account/user-detail/:id`。

---

## 3. `GET /api/v1/users/login-logs`

主文档已定稿；Vue **「登录日志」** 已对接到本接口。**Mock `POST /im-account/login-logs` 已从 `src/api` 移除**，勿再依赖。

---

## 4. 写操作 §3.5

自检：`login-password`、`fund-password`、`wallet/balance-adjust`、审计、bcmath、`409 concurrent_wallet_update` 等。

---

## 5. `related-by-ip` / `related-by-device`

响应字段与 **`hint`**、`shared_values`/`shared_ips` 与文档一致。

---

## 6. 群组 / 红包 / 转账 / 消息 / 手机相册

与 §3.6～§3.8 示例 JSON 对齐。**用户详情页**另需：`messages/c2c`、`messages/group`、`phone-album/config|list` — 清单见 **`好友列表页-Java后端待办.md` §11.2**。

---

## 7. 仍待产品 + 后端定案（主文档 §3.9）

| 业务 | 说明 |
|------|------|
| 充值/提现分页 | 专用 API 或复用 `wallet_transactions` 枚举，需在主文档单列一节 |
| 群组审计列表 | Mock `operation-logs`；可对 `admin_audit_logs` 分页 GET |

---

**注意：** API 的真相来源仍是 **`admin-api`** 源码；变更后主文档 §8 应同步更新。
