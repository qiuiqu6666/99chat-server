<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">
    <!--
      sangong-service 日志配置：
      - logs/sangong-service.log          主日志（INFO+，含业务 + 框架）
      - logs/sangong-service.error.log    ERROR 专用
      - 控制台 banner 关掉，全部日志只走滚动文件
      - logs/stdout.log 启动时由 start.sh 截断，仅承载 banner / 异常 System.out
      - 关键：使用 JaninoEventEvaluator + turboFilter 精准丢弃 Kafka client 在
        metadata bootstrap 阶段的两条高频噪音：
          "Connection to node -1 ... could not be established"
          "Bootstrap broker ... disconnected"
        这两条仅在客户端还不知道 broker 地址时出现，不影响消费恢复。
        Discovered group coordinator / Successfully joined 等关键消息仍为 INFO 输出。
    -->
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name" defaultValue="logs/sangong-service.log"/>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.error</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.error.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>20MB</maxFileSize>
            <maxHistory>14</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} : %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 全局 turboFilter：丢弃 Kafka client 在 metadata bootstrap 阶段的两条高频噪音 -->
    <turboFilter class="com.chat99.sangong.common.logging.KafkaNoiseFilter"/>

    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

    <!-- 业务包默认 INFO；可在此细调 -->
    <logger name="com.chat99.sangong" level="INFO"/>
    <!-- Kafka 客户端整体压到 WARN（bootstrap 噪音已被 turboFilter 抑制） -->
    <logger name="org.apache.kafka" level="WARN"/>
    <!-- NetworkClient 的 metadata bootstrap 循环直接收紧到 ERROR，turboFilter 双保险 -->
    <logger name="org.apache.kafka.clients.NetworkClient" level="ERROR"/>
    <!-- Spring Security 默认调高 -->
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="org.apache.coyote" level="WARN"/>
    <!-- SSE/异步：先压低噪音，等下个阶段再开放 -->
    <logger name="org.springframework.web.servlet.mvc.method.annotation" level="WARN"/>
    <logger name="org.springframework.web.context.request.async" level="WARN"/>
</configuration>